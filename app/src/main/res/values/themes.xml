<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HandicraftHorizonActionbar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/logo_bg</item>
        <item name="colorPrimaryVariant">@color/logo_bg</item>
        <item name="colorOnPrimary">@color/logo</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/logo</item>
        <item name="colorSecondaryVariant">@color/logo</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.HandicraftHorizon" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/logo_bg</item>
        <item name="colorPrimaryVariant">@color/logo_bg</item>
        <item name="colorOnPrimary">@color/logo</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/logo</item>
        <item name="colorSecondaryVariant">@color/logo</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>